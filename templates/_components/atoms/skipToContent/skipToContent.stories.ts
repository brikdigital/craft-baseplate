import { Meta, StoryObj } from '@storybook/server';

export default {
	title: 'Components/Atoms/Skip to content',
	argTypes: {},
} satisfies Meta;

export const Default: StoryObj = {
	name: 'Default',
	args: {},
};
