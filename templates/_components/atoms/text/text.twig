{% import '_macros/retcon.twig' as retcon %}
{% set variant = variant is defined and variant|length ? variant : 'default' %}

{% if html %}
	{% switch variant %}
		{% case 'darkBg' %}
			{{ retcon.darkBg(html) }}
		{% default %}
			{{ retcon.default(html) }}
	{% endswitch %}
{% endif %}